# Airflow runtime files (don't belong in version control)
logs/

# Machine-specific generated config
config/airflow.cfg

# Local environment file
.env

# Secrets — NEVER commit credentials or keys
*.json
*-key.json
application_default_credentials.json
